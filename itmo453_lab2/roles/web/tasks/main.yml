---
- name: Install NGINX
  apt:
    name: nginx
    state: present
    update_cache: yes

- name: Copy web page
  copy:
    src: index.html
    dest: /var/www/html/index.html

- name: Start and enable NGINX
  service:
    name: nginx
    state: started
    enabled: yes
